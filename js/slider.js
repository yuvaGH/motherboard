/* let countSlider = null;
let currentDot = 0;
let prevDot = 0;

window.onload = function() {
  document.getElementById('right').addEventListener('click', sliderRight);
  document.getElementById('left').addEventListener('click', sliderLeft);

  const slider = document.querySelector('.slider-containter');
  countSlider = slider.childElementCount;

  for (let i = 0; i < countSlider; i++) {
    slider.children[i].classList.add(`slider-${i + 1}`);
  }

}


function sliderRight() {
  slider.children[]
}

function sliderLeft() {
  console.log('left');
}
 */
